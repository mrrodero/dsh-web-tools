# Build-script allowlist for this project's own install (esbuild is a
# transitive dep of tsdown; onnxruntime-node postinstalls its native lib).
allowBuilds:
  esbuild: false
  onnxruntime-node: true
